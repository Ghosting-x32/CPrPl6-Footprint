#include"GrandSon2.h"

void GrandSon2::func()
{
	//m_A = 10;//私有继承时，父类中的public(共有属性)成员函数到子类变成privat(私有属性）----类内可以访问，类外不可访问
	//Son2中的m_A成员已变成private（私有属性），公有继承时，子类内不可以访问父类的privat(私有属性）成员

	//m_B = 10;//私有继承时，父类中的protected(保护属性)成员函数到子类变成privat(私有属性）----类内可以访问，类外不可访问
	//Son2中的m_B成员已变成private（私有属性），公有继承时，子类内不可以访问父类的privat(私有属性）成员
	
}
1．将范围[beg, end)间值等于old_val 的元素替换为new_val。
2．将范围[beg, end)间满足谓词pred 的元素替换为new_val。
3．将范围[beg, end)间的元素拷贝到目的序列dest 中，将其中值等于
old_val 的元素替换为new_val。
4．将范围[beg, end)间的元素拷贝到目的序列dest 中，将其中满足谓词pred
的元素替换为new_val。
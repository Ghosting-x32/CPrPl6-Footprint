对于string 类型的变量来说，因为string 类型本身接受无参数的初始化方
式，所以不论变量定义在函数内还是函数外都被默认初始化为空串。
对于内置类型int 来说，变量global_int 定义在所有函数体之外，根据C++
的规定，global_int 默认初始化为0；而变量local_int 定义在main 函数的内
部，将不被初始化，如果程序试图拷贝或输出未初始化的变量，将遇到一个未定义
的奇异值。
在本题的程序中，初始情况下a 的值是3、b 的值是4。decltype(a) c=a;
使用的是一个不加括号的变量，因此c 的类型就是a 的类型，即该语句等同于int
c=a;，此时c 是一个新整型变量，值为3。decltype((b)) d=a;使用的是一个
加了括号的变量，因此d 的类型是引用，即该语句等同于int &d=a;，此时d 是
变量a 的别名。
执行++c; ++d;时，变量c 的值自增为4，因为d 是a 的别名，所以d 自增1
意味着a 的值变成了4。当程序结束时，a、b、c、d 的值都是4。
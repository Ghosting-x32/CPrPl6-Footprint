(a)是非法的，函数的声明只包含一个参数，而函数的调用提供了两个参数，因
此无法编译通过。
(b)是合法的，字面值常量可以作为常量引用形参的值，字符'a'作为char 类型
形参的值也是可以的。
(c)是合法的，66 虽然是int 类型，但是在调用函数时自动转换为double 类
型。
(d) 是合法的， vec.begin() 和vec.end() 的类型都是形参所需的
vector<int>::iterator，第三个实参3.8 可以自动转换为形参所需的int 类型。